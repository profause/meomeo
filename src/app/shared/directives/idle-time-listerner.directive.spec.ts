import { IdleTimeListernerDirective } from './idle-time-listerner.directive';

describe('IdleTimeListernerDirective', () => {
  it('should create an instance', () => {
    const directive = new IdleTimeListernerDirective();
    expect(directive).toBeTruthy();
  });
});
